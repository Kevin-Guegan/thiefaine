{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Alerte creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('alerte') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
