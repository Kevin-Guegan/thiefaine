{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Utilisateurmobile list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Token</th>
                <th>Datecreation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('utilisateurmobile_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.token }}</td>
                <td>{% if entity.datecreation %}{{ entity.datecreation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('utilisateurmobile_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('utilisateurmobile_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('utilisateurmobile_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
